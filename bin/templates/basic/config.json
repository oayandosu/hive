{
    "env": "local",
    "db": {
        "connection": "mongodb://localhost/[%= name %]"
    },
    "debug": true,
    "name": "[%= name %]",
    "session": {
      "enabled": true,
      "secret": "[%= name %]-session-secret"
    },
    "view": {
      "extension": ".haml",
      "module": "hamljs"
    }
}